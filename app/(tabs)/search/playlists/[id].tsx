/** Route: `/(tabs)/search/playlists/[id]` — a playlist opened from the Search tab. Delegates to the shared {@link PlaylistPage} factory. */
export { default } from "@/components/pages/playlistPage";
