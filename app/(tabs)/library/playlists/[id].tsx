/** Route: `/(tabs)/library/playlists/[id]` — a playlist opened from the Library tab. Delegates to the shared {@link PlaylistPage} factory. */
export { default } from "@/components/pages/playlistPage";
